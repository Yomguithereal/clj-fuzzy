;; -------------------------------------------------------------------
;; clj-fuzzy Daitch-Mokotoff Soundex Tests
;; -------------------------------------------------------------------
;;
;;
;;   Author: PLIQUE Guillaume (Yomguithereal)
;;   Version: 0.1
;;
(ns clj-fuzzy.daitch-mokotoff-test
  (:require [clojure.test :refer :all]
            [clj-fuzzy.daitch-mokotoff :refer :all]))

; (deftest process-test
;   (is (= "R163" (process "Rupert")))
;   (is (= "R163" (process "Robert")))
;   (is (= "R150" (process "Rubin")))
;   (is (= "A261" (process "Ashcroft")))
;   (is (= "A261" (process "Ashcraft")))
;   (is (= "T522" (process "Tymczak")))
;   (is (= "P236" (process "Pfister")))
;   (is (= "A536" (process "Andrew")))
;   (is (= "W252" (process "Wozniak")))
;   (is (= "C423" (process "Callister"))))
